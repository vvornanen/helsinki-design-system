import React from 'react';

import { IconProps } from '../Icon.interface';

const [-replace-name-capital-]: React.FC<IconProps> = ({ className = '', style = {} }: IconProps) => (
  [-replace-svg-]
);

export default [-replace-name-capital-];
